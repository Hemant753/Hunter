<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User Profile - Fashion Hunter</title>
  <link
    href="https://cdn.jsdelivr.net/npm/remixicon@4.5.0/fonts/remixicon.css"
    rel="stylesheet"
/>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 font-sans">
  <div class="container mx-auto px-4 py-8">
    <a href="/fashionHunter/User/Home" class="font-bold text " >Home</a>
    <!-- Profile Card -->
    <div class="relative max-w-xl mx-auto bg-white shadow-lg rounded-lg overflow-hidden">

      <!-- Banner -->
      <div class="h-48 bg-gradient-to-r from-purple-400 via-pink-500 to-red-500"></div>
      
      <!-- Avatar -->
      <div class="absolute top-16 left-1/2 transform -translate-x-1/2">
        <div class="w-32 h-32 rounded-full border-4 border-white shadow-md">
          <div class="w-full h-full flex justify-center items-center rounded-full"> <i class="ri-user-fill text-8xl "></i> </div>        <!-- profile photo -->
        </div>
      </div>

      <!-- User Info -->
      <div class="pt-24 pb-8 px-8">
        <h1 class="text-2xl font-bold text-center text-gray-800"> <%= user.username %> </h1>
        <p class="text-center text-gray-600 mt-2">Member since  </p>

        <!-- User Details -->
        <div class="mt-6">
          <div class="flex justify-between items-center py-2 border-b">
            <span class="text-gray-600">Email:</span>
            <span class="text-gray-800 font-semibold"> <%= user.email %> </span>
          </div>
          <div class="flex justify-between items-center py-2 border-b">
            <span class="text-gray-600">Username:</span>
            <span class="text-gray-800 font-semibold"> <%= user.username %> </span>
          </div>
          <div class="flex justify-between items-center py-2 border-b">
            <span class="text-gray-600 mr-24 ">Address</span>
            <span class="text-gray-800 font-semibold text-sm text-left "> Lorem ipsum dolor sit amet, consectetur adipisicing elit. Quam alias nulla nobis, officia ab r adipisicing elit. Quam alias nulla nobis, officia ab </span>
          </div>
          <div class="flex justify-between items-center py-2">
            <span class="text-gray-600">Mobile :</span>
            <span class="text-gray-800 font-semibold"> <%= user.mobile %> </span>
          </div>
        </div>

        <!-- Bio -->
        <p class="mt-6 text-center text-gray-700 leading-relaxed">
          "Fashion is about dreaming and making other people dream." - A curated space to explore trends and express your unique style.
        </p>

        <!-- Action Buttons -->
        <div class="mt-6 flex justify-center gap-4">
          <a href="/fashionHunter/User/Edit/profile/<%= user.id %>"><button class="px-6 py-2 bg-purple-500 text-white font-semibold rounded-lg hover:bg-purple-600 transition">Edit Profile</button></a>
          <button class="px-6 py-2 border border-gray-300 text-gray-700 font-semibold rounded-lg hover:border-gray-400 transition">View Orders</button>
        </div>
      </div>
    </div>
  </div>

</body>
</html>