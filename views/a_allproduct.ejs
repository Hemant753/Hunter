<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Panel - Create Product</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <base href="/fashionHunter/admin/">
</head>

<body class="bg-gray-100 font-sans">
    <header class="bg-white shadow-md px-6 py-2 flex justify-between items-center cursor-pointer">
        <div class="container mx-auto px-4 py-2 flex justify-between items-center">
            <h1 class="text-xl font-bold">Admin Panel.</h1>
            <a href="#" class="text-black font-medium hover:text-black">Log Out</a>
        </div>
    </header>

    <div class="flex">
        <!-- Sidebar -->
        <aside class="min-w-[300px] h-screen bg-gray-200 p-4 text-center ">
            <ul class="space-y-2 mt-16 ">
                <li><a href="allproduct" class=" text-gray-500 hover:text-gray-700 font-bold ">All Products</a></li>
                <li><a href="newproduct" class=" text-gray-700 hover:text-gray-700  ">Create New Products</a></li>
            </ul>
        </aside>

        <main class=" products w-51 p-6">
            <div class="grid grid-cols-4 gap-4">

                    <% products.forEach((prod)=> { %>
                    <article class=" shadow text-center p-4 rounded-lg"
                    style="background-color: <%= prod.panelColor || '#ffffff' %>;">
                    <div class="h-52 flex items-center justify-center">
                        <img src="/image/products/<%= prod.images_1 %>" alt="Image of <%= prod.name %>"
                                class="h-[94%] w-[94%] object-cover rounded-md">
                    </div>
                    <div class=" py-1 px-3 h-18  flex flex-col items-start justify-center "
                    style="background-color: <%= prod.bottomColor || '#f8f9fa' %>; color: <%= prod.textColor || '#000000' %>;">
                        <div class="font-medium"><%= prod.name %></div>
                        <div class="flex w-full justify-between " >
                            <div class="font-medium   ">$<%= prod.FinalPrice %></div>
                            <div class="w-10 h-5 rounded-full font-bold text-sm  "style="background-color: <%= prod.DisColor || '#f8f9fa' %>; color: <%= prod.textColor || '#000000' %>;"><span> <%= prod.Discount %></span>% </div>
                        </div>
                    </div>
                    <a href="/fashionHunter/admin/Delete/Product/<%= prod.id %>">
                        <button class="text-red-500 text-xs hover:underline mt-2">Delete</button>
                    </a>
                </article>  
                <% }) %>

            </div>
        </main>

    </div>
</body>

</html>